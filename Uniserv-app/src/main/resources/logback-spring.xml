<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <!-- 修复：用 class 替代 deprecated 的 converterClass 属性 -->
    <conversionRule conversionWord="clr" class="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    class="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    class="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <!-- 通用常量定义 -->
    <property name="LOG_CHARSET" value="UTF-8"/>
    <property name="LOG_BASE_DIR" value="${LOG_PATH:-${user.dir}/logs}"/>
    <property name="LOG_FILE_NAME" value="uniserv"/>

    <!-- ========== 环境差异化日志格式 ========== -->
    <!-- 开发环境：精简格式（无日期、无PID，仅保留时分秒+TraceId+核心信息） -->
    <springProfile name="dev">
        <property name="COLOR_LOG_PATTERN"
                  value="%clr(%d{HH:mm:ss.SSS}){faint} %clr(%5p) %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr([%X{traceId}]){yellow} %clr(:){faint} %m%n%wEx"/>
        <!-- 开发环境文件日志也精简（无日期） -->
        <property name="FILE_LOG_PATTERN"
                  value="%d{HH:mm:ss.SSS} [%thread] %-5level [%X{traceId}] %logger{50} - %msg%n"/>
        <!-- 新增：开发环境全局日志级别为DEBUG（核心配置1） -->
        <property name="ROOT_LOG_LEVEL" value="INFO"/>
        <!-- 新增：开发环境控制台过滤级别为DEBUG（核心配置2） -->
        <property name="CONSOLE_THRESHOLD" value="INFO"/>
    </springProfile>

    <!-- 生产环境：完整格式（保留日期、PID、所有信息） -->
    <springProfile name="prod">
        <property name="COLOR_LOG_PATTERN"
                  value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr([%X{traceId}]){yellow} %clr(:){faint} %m%n%wEx"/>
        <!-- 生产环境文件日志完整格式 -->
        <property name="FILE_LOG_PATTERN"
                  value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{traceId}] %logger{50} - %msg%n"/>
        <!-- 新增：生产环境全局日志级别保持INFO -->
        <property name="ROOT_LOG_LEVEL" value="INFO"/>
        <!-- 新增：生产环境控制台过滤级别保持INFO -->
        <property name="CONSOLE_THRESHOLD" value="INFO"/>
    </springProfile>

    <!-- ========== 通用 Appender 配置 ========== -->
    <!-- 1. 控制台输出（带彩色，自动适配环境格式+环境过滤级别） -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>${LOG_CHARSET}</charset>
            <pattern>${COLOR_LOG_PATTERN}</pattern>
        </encoder>
        <!-- 修改：使用环境变量适配过滤级别，dev=DEBUG，prod=INFO -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${CONSOLE_THRESHOLD}</level>
        </filter>
    </appender>

    <!-- 2. 所有日志文件输出（按时间+大小分割，适配环境格式） -->
    <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_BASE_DIR}/${LOG_FILE_NAME}.log</file>
        <encoder>
            <charset>${LOG_CHARSET}</charset>
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_BASE_DIR}/history/${LOG_FILE_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
    </appender>

    <!-- 3. 错误日志单独输出（按时间+大小分割，适配环境格式） -->
    <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_BASE_DIR}/${LOG_FILE_NAME}-error.log</file>
        <encoder>
            <charset>${LOG_CHARSET}</charset>
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_BASE_DIR}/history/${LOG_FILE_NAME}-error-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>90</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- ========== 包级别日志控制（通用） ========== -->
    <logger name="com.uniserv" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE_ALL"/>
        <appender-ref ref="FILE_ERROR"/>
    </logger>
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.apache.ibatis" level="INFO"/>
    <logger name="com.alibaba" level="INFO"/>

    <!-- 根日志配置：修改为使用环境变量适配级别，dev=DEBUG，prod=INFO -->
    <root level="${ROOT_LOG_LEVEL}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE_ALL"/>
        <appender-ref ref="FILE_ERROR"/>
    </root>
</configuration>